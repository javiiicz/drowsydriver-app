import React, { useRef, useState, useEffect } from 'react';
import axios from 'axios';

const Camera = () => {

    return (
        <div>
            <h2 className='text-xl p-[5%]'>Try it Out</h2>
            <button>Hello</button>
        </div>
    );
};

export default Camera;
